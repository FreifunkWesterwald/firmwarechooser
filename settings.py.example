#!/usr/bin/env python3
import os.path

# Settings file

# Data path in application path
datapath = os.path.dirname(os.path.abspath(__file__)) + '/data'
# Data path external
# datapath = '/path/to/your/data'

# AdditionAL dict of mime types
mimetypes = {
    '.gz' : 'application/x-gzip',
    '.manifest' : 'text/plain'
}

# Index path
indexpath = 'static/index.json'

# README name
readmename = 'README.md'

# Delta between genration of index in minutes
timedelta = 10
